/*!
 * @file ILB.cpp
 *
 * @mainpage Arduino library for ILB - Input Light Sensor B.
 *
 * @section intro_sec Introduction
 *
 * This is documentation for the ILB
 *
 * @section dependencies Dependencies
 *
 * This library depends on <a
 * href="https://github.com/levkovigor/LTR390"> LTR390</a>
 * being present on your system. Please make sure you have installed the latest
 * version before using this library.
 *
 * @section author Author
 *
 * Written by @gotfredsen, with contributions from the open source community.
 *
 * @section license License
 *
 * MIT license.
 *
 */

#include "ILB.h"

/*!
    @brief  Constructor for sensor.
    @param  x
            bla bla.
    @param  y
            bla bla.
    @param  z
            zzz pin (using Arduino pin numbering), or -1 if not used.
    @return sensor object.
    @note   Call the object's begin() function before use -- buffer
            allocation is performed there!
*/
// ILB::ILB(uint8_t x, uint8_t z, int8_t z)
//     : xxx(x, -1), zzz(NULL), wire(twi ? twi : &Wire), buffer(NULL),
//       j(-1), k(-1), l(-1), csPin(-1), zzz(z)
// {
// }
 
/*!
    @brief  Destructor for ILB object.
*/
// ILB::~ILB(void) {
// }

/*!
    @brief  Initialize sensor.
    @param  x
            blabla.
    @param  y
            blabla.
    @param  z
            If true, and if the blabla.
    @return true on successful allocation/init, false otherwise.
            Well-behaved code should check the return value before
            proceeding.
    @note   MUST call this function before any drawing or updates!
*/
// bool ILB::begin(uint8_t x, uint8_t y, bool z) {
//   return true; // Success
// }

}
